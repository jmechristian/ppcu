"use client";

import InteriorPageLayout from "../InteriorPageLayout";

export default function CatalogPage() {
  return (
    <InteriorPageLayout activeItem="catalog">
      <section className="min-h-[520px] border border-gray-300 bg-white p-6">
        {/* Empty container for page-specific content */}
      </section>
    </InteriorPageLayout>
  );
}
